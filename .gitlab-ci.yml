run tests:
  stage: test
  image: python:3
  script:
    - pip install -r requirements.txt
    - pip install coverage
    - coverage run --source statkit -m unittest discover test
    - coverage report
    - coverage xml
  coverage: '/TOTAL.*\s([.\d]+)%/'
  artifacts:
    reports:
      cobertura: coverage.xml

pages:
  image: alpine:latest
  stage: deploy
  script:
  - apk add py3-pip py3-numpy py3-pandas
  - pip3 install pdoc3
  - mkdir public
  - pdoc --config latex_math=True --html --output-dir public statkit
  - mv public/statkit/* public/ && rmdir public/statkit/
  artifacts:
    paths:
    - public
  only:
  - master
